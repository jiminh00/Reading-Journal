server:
  port: 8080

spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: jdbc:mysql://localhost:3306/reading_journal?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Seoul
      username: root
      password: root

mybatis:
  configuration:
    map-underscore-to-camel-case: true